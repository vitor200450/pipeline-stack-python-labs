name: CI/CD Pipeline - Task Manager

on:
    push:
        branches: [ main, develop ]
    pull_request:
        branches: [ main ]
    workflow_dispatch:

env:
    PYTHON_VERSION: '3.11'
    NODE_VERSION: '18'

jobs:
    # Lint and Code Quality
    lint:
        name: Lint and Code Quality
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up Python
              uses: actions/setup-python@v5
              with:
                  python-version: ${{ env.PYTHON_VERSION }}

            - name: Cache dependencies
              uses: actions/cache@v4
              with:
                  path: ~/.cache/pip
                  key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
                  restore-keys: |
                      ${{ runner.os }}-pip-

            - name: Install dependencies
              run: |
                  python -m pip install --upgrade pip
                  pip install flake8 black isort
                  pip install -r requirements.txt

            - name: Run Black (Code Formatting)
              run: black --check --diff .

            - name: Run isort (Import Sorting)
              run: isort --profile black --check-only --diff .

            - name: Run flake8 (Linting)
              run: flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics

    # Unit Tests
    test:
        name: Unit Tests
        runs-on: ubuntu-latest
        needs: lint
        strategy:
            matrix:
                python-version: ['3.9', '3.10', '3.11']

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up Python ${{ matrix.python-version }}
              uses: actions/setup-python@v5
              with:
                  python-version: ${{ matrix.python-version }}

            - name: Cache dependencies
              uses: actions/cache@v4
              with:
                  path: ~/.cache/pip
                  key: ${{ runner.os }}-pip-${{ matrix.python-version }}-${{ hashFiles('**/requirements.txt') }}
                  restore-keys: |
                      ${{ runner.os }}-pip-${{ matrix.python-version }}-

            - name: Install dependencies
              run: |
                    python -m pip install --upgrade pip
                    pip install -r requirements.txt

            - name: Run tests with pytest
              run: |
                    pytest --verbose --tb=short --cov=. --cov-report=xml --cov-report=term

            - name: Upload coverage to Codecov
              if: matrix.python-version == '3.11'
              uses: codecov/codecov-action@v4
              with:
                  file: ./coverage.xml
                  flags: unittests
                  name: codecov-umbrella

    # Security Scan
    security:
        name: Security Scan
        runs-on: ubuntu-latest
        needs: lint
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up Python
              uses: actions/setup-python@v5
              with:
                  python-version: ${{ env.PYTHON_VERSION }}

            - name: Install dependencies
              run: |
                  python -m pip install --upgrade pip
                  pip install safety bandit
                  pip install -r requirements.txt

            - name: Run Safety (Dependency Vulnerability Check)
              run: safety check

            - name: Run Bandit (Security Linter)
              run: bandit -r . -f json -o bandit_report.json || true

            - name: Upload Bandit Report
              uses: actions/upload-artifact@v4
              if: always()
              with:
                  name: bandit-report
                  path: bandit_report.json

    # Build Docker Image
    build:
        name: Build Docker Image
        runs-on: ubuntu-latest
        needs: [test, security]
        outputs:
            image-tag: ${{ secrets.DOCKERHUB_USERNAME }}/task-manager:sha-${{ github.sha }}
            image-digest: ${{ steps.build.outputs.digest }}

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v3

            - name: Log in to Docker Hub
              if: github.event_name != 'pull_request'
              uses: docker/login-action@v3
              with:
                  username: ${{ secrets.DOCKERHUB_USERNAME }}
                  password: ${{ secrets.DOCKERHUB_PASSWORD }}

            - name: Extract metadata
              id: meta
              uses: docker/metadata-action@v5
              with:
                  images: ${{ secrets.DOCKERHUB_USERNAME }}/task-manager
                  tags: |
                        type=ref,event=branch
                        type=ref,event=pr
                        type=sha,prefix=sha-
                        type=raw,value=latest,enable={{is_default_branch}}

            - name: Build and Push Docker Image
              id: build
              uses: docker/build-push-action@v5
              with:
                  context: .
                  platforms: linux/amd64,linux/arm64
                  push: ${{github.event_name != 'pull_request'}}
                  tags: ${{steps.meta.outputs.tags}}
                  labels: ${{steps.meta.outputs.labels}}
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

    # Integration Tests
    integration-test:
        name: Integration Tests
        runs-on: ubuntu-latest
        needs: build
        if: github.event_name != 'pull_request'  # Skip integration tests for PRs

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Build local Docker Image for testing
              run: |
                docker build -t local-task-manager:test .

            - name: Start application container
              run: |
                docker run -d --name integration-test-app -p 8000:8000 local-task-manager:test

            - name: Wait for the application to be ready
              run: |
                timeout 60 bash -c 'until curl -s http://localhost:8000/health; do sleep 5; done'

            - name: Run API integration tests
              run: |
                # Test health endpoint
                curl -f http://localhost:8000/api/health

                # Test task creation
                TASK_ID=$(curl -s -X POST http://localhost:8000/api/tasks \
                            -H "Content-Type: application/json" \
                            -d '{"title":"Integration Test Task","priority":"high"}' | \
                            python -c "import sys, json; print(json.load(sys.stdin)['id'])")

                # Test task retrieval
                curl -f http://localhost:8000/api/tasks/$TASK_ID

                # Test task update
                curl -f -X PUT http://localhost:8000/api/tasks/$TASK_ID \
                            -H "Content-Type: application/json" \
                            -d '{"completed":true}'

                # Test statistics
                curl -f http://localhost:8000/api/stats

                # Test task deletion
                curl -f -X DELETE http://localhost:8000/api/tasks/$TASK_ID

            - name: Test frontend acessibility
              run: |
                  # Test that frontend loads
                  curl -f http://localhost:8000/

                  # Test static files
                  curl -f http://localhost:8000/static/style.css
                  curl -f http://localhost:8000/static/script.js

            - name: Cleanup integration test container
              if: always()
              run: docker stop integration-test-app && docker rm integration-test-app

    # Performance Tests
    performance-test:
        name: Performance Tests
        runs-on: ubuntu-latest
        needs: integration-test
        if: github.event_name != 'pull_request'  # Skip performance tests for PRs

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Setup Python
              uses: actions/setup-python@v5
              with:
                  python-version: ${{ env.PYTHON_VERSION }}

            - name: Install dependencies
              run: |
                pip install locust

            - name: Build local Docker Image for performance testing
              run: |
                docker build -t local-task-manager:test .

            - name: Run performance tests
              run: |
                # Start the application container
                docker run -d --name perf-test-app -p 8000:8000 local-task-manager:test

                # Wait for the application to be ready
                timeout 60 bash -c 'until curl -s http://localhost:8000/health; do sleep 5; done'

                # Run basic load test
                locust -f .github/workflows/locustfile.py --host http://localhost:8000 --users 50 --spawn-rate 5 --run-time 2m --headless --only-summary

            - name: Cleanup
              if: always()
              run: docker stop perf-test-app && docker rm perf-test-app

    # Deploy to Staging
    deploy-staging:
        name: Deploy to Staging
        runs-on: ubuntu-latest
        needs: [integration-test, performance-test]
        if: github.ref == 'refs/heads/develop' && github.event_name == 'push'  # Only deploy to staging on pushes to develop
        environment:
            name: staging
            url: https://task-manager-staging.example.com  # Replace with your staging URL

        steps:
            - name: Deploy to staging
              run: |
                    echo "Deploying to staging environment..."
                    echo "Image: ${{ needs.build.outputs.image-tag }}"
                    # Add your deployment commands here, e.g., kubectl apply, docker-compose up, etc.

    # Deploy to Production
    deploy-production:
        name: Deploy to Production
        runs-on: ubuntu-latest
        needs: [integration-test, performance-test]
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        environment:
            name: production
            url: https://task-manager.example.com  # Replace with your production URL

        steps:
            - name: Deploy to production
              run: |
                    echo "Deploying to production environment..."
                    echo "Image: ${{ needs.build.outputs.image-tag }}"
                    # Add your deployment commands here, e.g., kubectl apply, docker-compose up, etc.

    # Notification
    notify:
        name: Notify Deployment
        runs-on: ubuntu-latest
        needs: [deploy-staging, deploy-production]
        if: always() && (needs.deploy-staging.result != 'skipped' || needs.deploy-production.result != 'skipped')

        steps:
            - name: Notify success
              if: needs.deploy-staging.result == 'success' || needs.deploy-production.result == 'success'
              run: |
                    echo "Deployment successful!"
                    echo "Staging: ${{needs.deploy-staging.result}}"
                    echo "Production: ${{needs.deploy-production.result}}"
                    # Add your notification commands here, e.g., send a message to Slack, email, etc.

            - name: Notify failure
              if: needs.deploy-staging.result == 'failure' || needs.deploy-production.result == 'failure'
              run: |
                    echo "Deployment failed!"
                    echo "Staging: ${{needs.deploy-staging.result}}"
                    echo "Production: ${{needs.deploy-production.result}}"
                    # Add your notification commands here, e.g., send a message to Slack, email, etc.
